/** @type {import('next').NextConfig} */
const nextConfig = {
  // Example: enable React strict mode if you want
  reactStrictMode: true,

  // If you need any custom config, add it here
  // e.g. image domains, experimental flags, rewrites, etc.
};

module.exports = nextConfig;
